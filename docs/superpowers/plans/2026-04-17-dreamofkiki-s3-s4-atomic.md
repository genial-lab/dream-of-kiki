# dreamOfkiki S3-S4 Atomic Plan

> **Pour agents autonomes :** SKILL REQUIS — utiliser `superpowers:subagent-driven-development` (recommandé) ou `superpowers:executing-plans` pour implémenter tâche par tâche. Les steps utilisent la syntaxe checkbox (`- [ ]`) pour le tracking.

**Goal** : raffiner les 6 tâches S3.1-S4.2 du plan global en **tâches atomiques exécutables** (2-5 minutes par step), style S1.1-S2.3 qui ont déjà livré 14 commits avec succès.

**Architecture** : 6 tasks atomiques (S3.1, S3.2, S3.3, S3.4, S4.1, S4.2) produisant chacune 1 commit (ou 2 pour S3.4 TDD). Mix humain-automatisable : S3.2 et S3.3 ont parties manuelles documentées ; S3.1/S3.4/S4.1/S4.2 sont full-code. Total attendu : 7-9 commits.

**Tech Stack** : Python 3.12+ avec uv, pytest + hypothesis, SQLite, YAML (eval-matrix schema), pyyaml (lecture config), Markdown (docs + interfaces).

**Préréquis** :
- 14 commits dreamOfkiki déjà en place (dernier : `1a02aa8 docs(tcol): log S2 status and Branch A lock`)
- Gate G1 locked Branch A (Studyforrest feasibility)
- 8 typed Protocols existants dans `kiki_oniric/core/primitives.py`
- 5 tests passants, coverage 98%
- Working tree propre

---

## Convention commits (validator-enforced)

- Subject line **≤50 characters**, format `<type>(<scope>): <description>`
- Types: `feat|fix|refactor|test|docs|ci|chore|style|perf`
- Scope ≥3 chars (single letters rejected)
- Body lines ≤72 chars, 2-3 paragraphs required
- **NO AI attribution** (no `Co-Authored-By`, no "Generated by")
- NO `--no-verify`

---

## File structure après S3-S4

```
dreamOfkiki/
├── docs/
│   ├── interfaces/
│   │   ├── primitives.md             ← S3.1 (contrat humain-lisible)
│   │   └── eval-matrix.yaml          ← S3.1 (machine-readable)
│   ├── osf-preregistration-draft.md  ← S3.2
│   └── drafts/
│       └── C-v0.5-changelog.md       ← S4.2
├── harness/
│   └── benchmarks/
│       ├── __init__.py               ← S3.4
│       └── retained/
│           ├── __init__.py           ← S3.4
│           ├── items.jsonl           ← S3.4 (500 items gelés)
│           ├── items.jsonl.sha256    ← S3.4 (hash gel)
│           └── retained.py           ← S3.4 (loader + hash check)
├── ops/
│   └── formal-reviewer-recruitment.md ← S3.3
├── tests/
│   └── unit/
│       ├── test_eval_matrix.py       ← S3.1 (parser + validation)
│       └── test_retained_benchmark.py ← S3.4 (loader + hash)
└── docs/interfaces/
    └── fmri-schema.yaml              ← S4.1
```

---

# Task S3.1 — Interfaces formels (primitives.md + eval-matrix.yaml)

**Goal** : locker les **contrats cross-track** que C↔A↔T-Ops doivent respecter. Deux livrables : `primitives.md` (humain-lisible, référence pour reviewers) et `eval-matrix.yaml` (machine-readable, consommé par harness dispatcher).

**Files:**
- Create : `docs/interfaces/primitives.md`
- Create : `docs/interfaces/eval-matrix.yaml`
- Create : `tests/unit/test_eval_matrix.py`
- Create : `harness/config/__init__.py`
- Create : `harness/config/eval_matrix.py`

## Step S3.1.1 — Créer docs/interfaces/primitives.md

- [ ] Créer `docs/interfaces/primitives.md` avec exactement :

```markdown
# Primitives Interface Contract

**Version** : C-v0.3.1+STABLE (draft, bump to C-v0.5.0 at S4.2)
**Owners** : Track C (design) + Track A (implementation)
**Locked** : S2 → S4 (evolves with framework)

Any substrate instantiating the dreamOfkiki framework MUST implement
these 8 typed Protocols. Reference: `kiki_oniric/core/primitives.py`.

---

## Awake → Dream primitives

### α — AlphaStreamProtocol (P_max only)

Raw forward-pass traces firehose.

**Python Protocol**: `kiki_oniric.core.primitives.AlphaStreamProtocol`

**Methods**:
- `append_trace(tokens, activations, attention, errors) -> None`
- `iter_traces() -> Iterator[dict[str, NDArray]]`

**Storage contract**: ring buffer, LIFO rotation, mmap-backed, cap
10 GB (configurable per profile).

**Activation**: P_max only. Calling in P_min/P_equ profile is a
logic error (caller must check profile).

### β — BetaBufferProtocol (all profiles)

Curated episodic buffer with saillance-gated append.

**Python Protocol**: `kiki_oniric.core.primitives.BetaBufferProtocol`

**Methods**:
- `append_record(context, outcome, saillance, ts) -> record_id`
- `fetch_unconsumed(limit) -> list[dict]`
- `mark_consumed(record_ids, de_id) -> None`

**Storage contract**: SQLite append-log with index on
`(saillance DESC, consumed_by NULLS FIRST)`.

**Invariant I1 contract**: every record must have
`consumed_by_DE_id` set before purge. Enforcement: hourly cron
(T-Ops.6) + purge policy.

### γ — GammaSnapshotProtocol (fallback)

Weights-only snapshot pointer.

**Python Protocol**: `kiki_oniric.core.primitives.GammaSnapshotProtocol`

**Methods**:
- `get_checkpoint_path() -> Path`
- `get_checkpoint_sha256() -> str`

**Usage**: fallback / diagnostic. P_min/P_equ/P_max rely on β+δ
primarily.

### δ — DeltaLatentsProtocol (P_equ, P_max)

Hierarchical latent snapshots across ortho species.

**Python Protocol**: `kiki_oniric.core.primitives.DeltaLatentsProtocol`

**Methods**:
- `snapshot(species_activations) -> snapshot_id`
- `get_recent(n) -> list[dict[str, NDArray]]`

**Storage contract**: ring buffer N=256 snapshots.

---

## Dream → Awake channels

### Canal 1 — WeightDeltaChannel

Parametric consolidation output, applied via swap.

**Python Protocol**: `kiki_oniric.core.primitives.WeightDeltaChannel`

**Methods**:
- `apply(lora_delta, fisher_bump=None) -> None`

**Constraint**: must satisfy invariants S1 (retained non-regression)
+ S2 (finite values). Enforcement: swap protocol guards.

### Canal 2 — LatentSampleChannel

Generative replay / data augmentation queue.

**Python Protocol**: `kiki_oniric.core.primitives.LatentSampleChannel`

**Methods**:
- `enqueue(species, latent_vector, provenance) -> None`
- `dequeue() -> dict | None`

**Constraint**: must satisfy invariant I3 (distributional drift
bounded, KL ≤ ε_drift).

### Canal 3 — HierarchyChangeChannel

Topology diff applied atomically at swap time.

**Python Protocol**: `kiki_oniric.core.primitives.HierarchyChangeChannel`

**Methods**:
- `apply_diff(diff: list[tuple[str, dict]]) -> None`

**Constraint**: must satisfy invariant S3 (validate_topology
passes on post-state).

### Canal 4 — AttentionPriorChannel

Meta-cognitive attention guidance.

**Python Protocol**: `kiki_oniric.core.primitives.AttentionPriorChannel`

**Methods**:
- `set_prior(prior: NDArray) -> None`
- `get_prior() -> NDArray | None`

**Constraint**: must satisfy invariant S4 (each component in [0,1],
sum ≤ budget_attention).

---

## Contract test suite

Contract tests are in `tests/conformance/axioms/` and
`tests/conformance/invariants/`. Each protocol must pass:

1. **Signature check** (`test_dr3_substrate.py`): runtime-checkable,
   8 protocols declared.
2. **Axiom check** (S5+): DR-0..DR-4 property tests pass for concrete
   implementations.
3. **Invariant check** (S5+): I1/I2/I3/S1/S2/S3/S4/K1/K3/K4
   enforceable on runtime.

Failing any contract test blocks the substrate from claiming
framework conformance (DR-3 Conformance Criterion, framework §6.2).
```

- [ ] Commit S3.1.1 (sera commité avec les autres parties en S3.1.4)

## Step S3.1.2 — Créer docs/interfaces/eval-matrix.yaml

- [ ] Créer `docs/interfaces/eval-matrix.yaml` avec exactement :

```yaml
# Eval matrix stratification rules
# Referenced by harness/config/eval_matrix.py
# Version: C-v0.3.1+STABLE

version: "C-v0.3.1+STABLE"

bump_rules:
  PATCH:
    description: "Only the touched axis metric, P_equ only, 1 seed"
    obligatory:
      - axis: touched
        profile: P_equ
        seeds: 1
    optional: []

  MINOR:
    description: "Full 8-metric suite on P_equ with 3 seeds"
    obligatory:
      - axis: all_8_metrics
        profile: P_equ
        seeds: 3
    optional:
      - axis: all_8_metrics
        profiles: [P_min, P_max]
        seeds: 3
        deferred_to: next_MAJOR

  MAJOR:
    description: "Full grid: 8 metrics x 3 profiles x 3 seeds = 72 runs"
    obligatory:
      - axis: all_8_metrics
        profiles: [P_min, P_equ, P_max]
        seeds: 3
    optional:
      - axis: sensitivity_sweeps

  EC_change:
    description: "Only re-run published-result metrics"
    obligatory:
      - axis: published_metrics
        profile: current
        seeds: current
    optional: []

publication_ready_gate:
  description: "Additional criteria on top of GREEN mode"
  requires:
    coverage: "100% stratified cells in last MAJOR bump"
    seeds_per_cell: 3
    retained_regression_max_pct: 1.0
    consecutive_zero_blocking_days: 7
    dualver_status: "+STABLE"
    pre_submission_reviews_min: 1
    axioms_proven:
      - DR-0
      - DR-1
      - DR-2  # or DR-2' if fallback adopted
      - DR-3
      - DR-4
    ablation_complete:
      - P_min
      - P_equ
      - P_max
    paper_draft: complete

metrics:
  M1.a:
    name: forgetting_rate
    family: continual_learning
    determinism: seeded_rng
    topology_sensitive: false
  M1.b:
    name: avg_accuracy
    family: continual_learning
    determinism: seeded_rng
    topology_sensitive: false
  M2.b:
    name: rsa_fmri_alignment
    family: representational
    determinism: cpu_nilearn_seeded
    topology_sensitive: false
    data_source: studyforrest  # Branch A locked S2.1
  M3.a:
    name: flops_ratio
    family: efficiency
    determinism: mlx_static_profile
    topology_sensitive: true
  M3.b:
    name: offline_gain
    family: efficiency
    determinism: flops_equiv_wallclock
    topology_sensitive: true
    pivot: true  # shared between E3 and E4 targets
  M3.c:
    name: energy_per_episode
    family: efficiency
    determinism: calibrated_proxy
    topology_sensitive: true
  M4.a:
    name: recomb_quality
    family: emergence
    determinism: frozen_scorer_qwen3_5_9b_q4km
    topology_sensitive: false
  M4.b:
    name: structure_discovery
    family: emergence
    determinism: permutation_test_seeded
    topology_sensitive: false
```

## Step S3.1.3 — Écrire test failing pour eval_matrix.py

- [ ] Créer `tests/unit/test_eval_matrix.py` avec exactement :

```python
"""Unit tests for eval matrix config parser.

Validates that docs/interfaces/eval-matrix.yaml loads correctly
and exposes the stratification rules for the stratified matrix
dispatcher (T-Ops.2).
"""
from pathlib import Path

import pytest

from harness.config.eval_matrix import EvalMatrix, load_eval_matrix


REPO_ROOT = Path(__file__).resolve().parents[2]
MATRIX_PATH = REPO_ROOT / "docs" / "interfaces" / "eval-matrix.yaml"


@pytest.fixture
def matrix() -> EvalMatrix:
    return load_eval_matrix(MATRIX_PATH)


def test_matrix_version_starts_with_C_prefix(matrix: EvalMatrix) -> None:
    assert matrix.version.startswith("C-v")


def test_matrix_has_all_four_bump_rules(matrix: EvalMatrix) -> None:
    assert set(matrix.bump_rules.keys()) == {
        "PATCH",
        "MINOR",
        "MAJOR",
        "EC_change",
    }


def test_major_bump_requires_all_three_profiles(
    matrix: EvalMatrix,
) -> None:
    major = matrix.bump_rules["MAJOR"]
    obligatory = major["obligatory"][0]
    assert obligatory["profiles"] == ["P_min", "P_equ", "P_max"]
    assert obligatory["seeds"] == 3


def test_all_8_metrics_declared(matrix: EvalMatrix) -> None:
    expected = {"M1.a", "M1.b", "M2.b", "M3.a", "M3.b", "M3.c",
                "M4.a", "M4.b"}
    assert set(matrix.metrics.keys()) == expected


def test_m3_b_is_pivot_metric(matrix: EvalMatrix) -> None:
    assert matrix.metrics["M3.b"].get("pivot") is True


def test_m2_b_data_source_is_studyforrest(matrix: EvalMatrix) -> None:
    # G1 Branch A locked at S2.1
    assert matrix.metrics["M2.b"]["data_source"] == "studyforrest"
```

- [ ] Lancer le test, vérifier qu'il échoue :

```bash
uv run pytest tests/unit/test_eval_matrix.py -v --no-cov
```

Expected: FAIL with `ModuleNotFoundError: harness.config`.

## Step S3.1.4 — Implémenter harness/config/eval_matrix.py

- [ ] Créer `harness/config/__init__.py` (vide).

- [ ] Créer `harness/config/eval_matrix.py` avec exactement :

```python
"""Eval matrix configuration loader.

Parses docs/interfaces/eval-matrix.yaml and exposes the stratification
rules as a typed EvalMatrix dataclass for the harness dispatcher.

Reference: docs/interfaces/eval-matrix.yaml
         : docs/specs/2026-04-17-dreamofkiki-framework-C-design.md §8.2
"""
from dataclasses import dataclass, field
from pathlib import Path
from typing import Any

import yaml


@dataclass(frozen=True)
class EvalMatrix:
    """Parsed eval-matrix.yaml with typed accessors."""

    version: str
    bump_rules: dict[str, dict[str, Any]]
    publication_ready_gate: dict[str, Any]
    metrics: dict[str, dict[str, Any]]


def load_eval_matrix(path: Path) -> EvalMatrix:
    """Load and validate eval-matrix.yaml."""
    if not path.exists():
        raise FileNotFoundError(f"eval-matrix.yaml not found at {path}")

    with path.open("r", encoding="utf-8") as fh:
        raw = yaml.safe_load(fh)

    required_top_keys = {
        "version",
        "bump_rules",
        "publication_ready_gate",
        "metrics",
    }
    missing = required_top_keys - raw.keys()
    if missing:
        raise ValueError(
            f"eval-matrix.yaml missing top-level keys: {missing}"
        )

    return EvalMatrix(
        version=raw["version"],
        bump_rules=raw["bump_rules"],
        publication_ready_gate=raw["publication_ready_gate"],
        metrics=raw["metrics"],
    )
```

- [ ] Lancer le test, vérifier qu'il passe :

```bash
uv run pytest tests/unit/test_eval_matrix.py -v --no-cov
```

Expected: 6 passed.

- [ ] Lancer le full test suite pour vérifier coverage :

```bash
uv run pytest
```

Expected: 11 tests passés, coverage ≥90%.

## Step S3.1.5 — Commit S3.1

- [ ] Commit :

```bash
git add docs/interfaces/ harness/config/ tests/unit/test_eval_matrix.py
git commit -m "feat(config): lock interfaces primitives + eval matrix"
```

Subject : 49 chars (≤50).

Body (3 paragraphs) à rédiger :

1. Explique que cette tâche locke les contrats interfaces `primitives.md` (humain-lisible) et `eval-matrix.yaml` (machine-readable). Référence le contrat de versioning DualVer (C-v0.3.1+STABLE pour l'instant, bump C-v0.5.0 en S4.2).

2. Explique le parser `EvalMatrix` dataclass : 4 bump rules (PATCH/MINOR/MAJOR/EC_change), 8 metrics déclarées avec determinism strategy par métrique (Branch A Studyforrest verrouillée pour M2.b).

3. Explique la couverture tests : 6 tests unitaires validant schema, bump rules, metrics (M1.a..M4.b) et pivot M3.b. Intègre DR-3 Conformance Criterion partiel (typed contract exposé).

---

# Task S3.2 — OSF pre-registration H1-H4

**Goal** : pre-enregistrer les 4 hypothèses H1-H4 sur OSF (Open Science Framework) pour prévenir p-hacking et lock l'analysis plan avant les expériences. **Tâche mi-code mi-manuelle** : le code génère le draft, l'action humaine upload.

**Files:**
- Create : `docs/osf-preregistration-draft.md` (source de vérité du pre-reg)
- Create : `docs/osf-upload-checklist.md` (checklist action humaine)

## Step S3.2.1 — Créer docs/osf-preregistration-draft.md

- [ ] Créer `docs/osf-preregistration-draft.md` avec exactement :

```markdown
# OSF Pre-Registration — dreamOfkiki Cycle 1

**Project** : dreamOfkiki
**PI** : Clement Saillant (L'Electron Rare)
**Date drafted** : 2026-04-17 (S3)
**Lock date target** : S3 before experiments begin
**OSF URL** : https://osf.io/XXXX (fill after upload)
**DOI** : 10.17605/OSF.IO/XXXX (fill after upload)

## 1. Study design

A within-architecture ablation comparing three dream-consolidation
profiles (P_min, P_equ, P_max) of a kiki-oniric linguistic model
against a no-dream baseline, on mega-v2 continual learning tasks
and Studyforrest fMRI representational alignment.

## 2. Hypotheses

### H1 — P_equ reduces catastrophic forgetting

**Statement** : P_equ reduces forgetting rate (M1.a) by ≥10% vs
no-dream baseline on mega-v2 continual learning benchmark.

**Operationalization** :
- M1.a = (acc_task_1_initial − acc_task_1_after_task_N) /
        acc_task_1_initial
- Measured across 25 domains of mega-v2
- Statistical test : paired t-test, one-sided,
  α = 0.05, n = 3 seeds minimum
- Reject H0 if p < 0.05 AND effect size ≥ 10% relative improvement

### H2 — P_max shows diminishing returns vs P_equ

**Statement** : P_max provides marginal improvement ≤5% over P_equ
on M1.a (law of diminishing returns).

**Operationalization** :
- Compare M1.a(P_max) vs M1.a(P_equ)
- Statistical test : equivalence test (TOST), ε_equivalence = 5%
- Conclude H2 supported if CI falls within [−5%, +5%]

### H3 — Monotonic RSA fMRI alignment

**Statement** : M2.b representational alignment increases
monotonically across profiles : P_min < P_equ < P_max.

**Operationalization** :
- M2.b = Pearson correlation between RDM(kiki activations) and
        RDM(Studyforrest fMRI ROIs STG/IFG/AG)
- Data : Studyforrest (Branch A locked S2.1 G1)
- Statistical test : Jonckheere-Terpstra monotonic trend test,
  α = 0.05
- Reject H0 if p < 0.05 AND pairwise P_min < P_equ and
  P_equ < P_max both significant

### H4 — Dream compute overhead is bounded

**Statement** : M3.c energy proxy ratio stays ≤2× awake for P_equ
(deployment viability).

**Operationalization** :
- M3.c = FLOPs-derived energy proxy per dream-episode
- Ratio = energy(dream) / energy(awake) over equivalent wall-clock
- Reject H4 violation if 95% CI upper bound exceeds 2.0

## 3. Pre-specified analyses

### Primary analyses

- H1 : paired t-test, one-sided
- H2 : TOST equivalence test, bidirectional
- H3 : Jonckheere-Terpstra monotonic trend test
- H4 : one-sample t-test against ratio = 2.0

### Sample size / power

- n = 3 seeds per (profile, metric) cell (minimum for PUBLICATION-
  READY gate)
- Power analysis based on pilot runs S5-S7 — if power < 0.8 for
  any hypothesis, increase to 5 seeds

### Multiple comparison correction

- Bonferroni correction across H1-H4 family : α_per_hypothesis =
  0.05 / 4 = 0.0125

## 4. Data exclusion rules

- Runs that violate any BLOCKING invariant (I1, I2, S1, S2, S3, K1,
  K4) are excluded and logged to `aborted-swaps/` for forensic
  review
- Runs with `EC_state != STABLE` at time of measurement are
  excluded from hypothesis testing (they can be re-run after
  EC stabilization)
- Seeds that fail repro smoke test (R1 contract) are excluded and
  re-run with deterministic seed fix

## 5. Deviations from pre-registration

Any post-hoc deviations from this analysis plan will be :
1. Documented in a separate `docs/osf-deviations-<date>.md`
2. Reported in paper 1 as exploratory (not confirmatory)
3. Linked from OSF project page

## 6. Data and code availability

- Code : https://github.com/electron-rare/dreamOfkiki (MIT)
- Models : https://huggingface.co/clemsail/kiki-oniric-*
- Raw runs : Zenodo DOI (at S20-S22)
- Harness : pip-installable `dreamOfkiki.harness`

## 7. Contact

Clement Saillant — clement@saillant.cc — L'Electron Rare, France

---

**Lock this document before any experiment begins.**
```

## Step S3.2.2 — Créer docs/osf-upload-checklist.md

- [ ] Créer `docs/osf-upload-checklist.md` avec exactement :

```markdown
# OSF Pre-Registration Upload Checklist

This is a **human action checklist** for the S3 OSF pre-registration.

Source document : `docs/osf-preregistration-draft.md`

## Checklist

- [ ] Login to https://osf.io with account (or create if needed)
- [ ] Create project "dreamOfkiki" with description from README.md
- [ ] Add wiki section "Pre-registration H1-H4"
- [ ] Upload `docs/osf-preregistration-draft.md` content to OSF
  pre-registration form using the "Standard Pre-Data Collection"
  template
- [ ] Lock the pre-registration (timestamped registration)
- [ ] Copy OSF project URL to `docs/osf-preregistration-draft.md`
  header
- [ ] Copy OSF DOI to `docs/osf-preregistration-draft.md` header
- [ ] Commit the URL+DOI update :

      ```bash
      git add docs/osf-preregistration-draft.md
      git commit -m "docs(osf): lock H1-H4 registration [DOI: ...]"
      ```

- [ ] Share OSF project link internally with T-Col.4 pre-submission
  reviewers network
- [ ] Update `ops/tcol-outreach-plan.md` status log with lock
  confirmation

## Estimated time

- OSF account setup : 10 min (one-time)
- Project creation + upload : 20 min
- Lock + URL/DOI update : 5 min
- **Total** : ~35 min human time

## Verification

After lock, verify :
- OSF project URL resolves
- DOI resolves via https://doi.org/10.17605/OSF.IO/XXXX
- Registration is timestamped and immutable
- H1-H4 hypotheses present and match `draft.md`

## Blocking conditions

- If OSF service is down : defer to next business day, do not
  proceed with experiments until pre-reg locked
- If template doesn't fit : use the generic "Open-Ended
  Registration" and attach the draft.md as supplementary file
```

## Step S3.2.3 — Commit S3.2

- [ ] Commit :

```bash
git add docs/osf-preregistration-draft.md docs/osf-upload-checklist.md
git commit -m "docs(osf): draft H1-H4 pre-registration"
```

Subject : 41 chars.

Body (2 paragraphs) :

1. Explique la raison d'être : OSF pre-reg préviens p-hacking et
   lock l'analysis plan avant les expériences S5+. Hypothèses
   opérationnalisées avec tests statistiques précisés (paired
   t-test H1, TOST H2, Jonckheere-Terpstra H3, one-sample H4) et
   Bonferroni correction (α = 0.0125).

2. Explique la séparation draft vs upload : draft locké dans git
   maintenant, upload OSF action humaine via checklist
   `osf-upload-checklist.md`. Lock effectif à l'issue de l'upload.

---

# Task S3.3 — Recruter relecteur formel DR-2 (Q_CR.1 b)

**Goal** : établir un tracker pour le recrutement d'un relecteur humain externe de la preuve DR-2 compositionnalité. Mi-code mi-humain : code produit le tracker + email template, user envoie les demandes.

**Files:**
- Create : `ops/formal-reviewer-recruitment.md`
- Create : `ops/formal-reviewer-email-template.md`

## Step S3.3.1 — Créer tracker recrutement

- [ ] Créer `ops/formal-reviewer-recruitment.md` avec exactement :

```markdown
# Formal Reviewer Recruitment (DR-2 Compositionality Proof)

**Context** : critic finding Q_CR.1 b — DR-2 proof needs external
peer review beyond sub-agent `critic`. Target recruitment S3-S5,
draft circulation S6 (G3-draft milestone), final review S6-S8.

## Target profile

- TCS / category theory / monoid structures familiarity (ideal)
- OR cognitive modeling with formal background (acceptable)
- Available for 2-3 hours of proof review work
- Willing to accept courtesy co-authorship on Paper 1 as
  compensation for formal review contribution

## Candidates

| Priority | Name/Handle | Profile | Contact method | Status |
|----------|-------------|---------|----------------|--------|
| 1 | [TBD1] | [profile] | [email/linkedin] | TODO_S3 |
| 2 | [TBD2] | [profile] | [email/linkedin] | TODO_S3 |
| 3 | [TBD3] | [profile] | [email/linkedin] | TODO_S4 |
| Fallback | Sub-agent `critic` + `validator` | Automated | N/A | Always available |

**Action user (S3)** : remplir ligne par ligne les candidats réels du
réseau académique, remplacer TBD par noms + contacts.

## Outreach sequence

- **S3 day 1** : send email to priority 1 using template below
- **S3 day 3** : if no reply, send to priority 2
- **S4 day 1** : if still no reply, send to priority 3
- **S5** : confirmation + NDA / courtesy agreement
- **S6** : circulate DR-2 draft (G3-draft milestone)
- **S7-S8** : review iteration
- **S8** : final review → G3 gate decision

## Tracking log

| Date | Action | Status | Notes |
|------|--------|--------|-------|
| 2026-04-17 | Tracker created | DONE | S3.3.1 |
| TBD | Priority 1 contacted | TODO | |

## Fallback protocol (if no human reviewer by S6)

If no human reviewer confirmed by S6, **Pivot B partial** activates :
- DR-2 proof reviewed by sub-agent `critic` + `validator` only
- Paper 1 framed as "formal-leaning" rather than "formally proven"
- Target journal adjusted : PLoS Comp Bio instead of Nature HB
- Document decision in `docs/proofs/g3-decision-log.md` at S8
```

## Step S3.3.2 — Créer email template

- [ ] Créer `ops/formal-reviewer-email-template.md` avec exactement :

```markdown
# Email Template — Formal Reviewer Request (DR-2)

**Variables to fill** : `[NAME]`, `[CONTEXT_HOOK]`, `[DATE]`

---

Subject: Brief formal-proof review request — compositional dream
architecture for AI

Dear [NAME],

[CONTEXT_HOOK — e.g., "We met at X conference" / "I read your paper
on Y" / "Colleague Z suggested I reach out"].

I'm finishing up a research program called **dreamOfkiki** — a
formal framework axiomatizing dream-based knowledge consolidation in
artificial cognitive systems. The framework defines four primitive
operations (replay, downscale, restructure, recombine) as a
monoid-like algebraic structure over cognitive state transitions,
with a compositionality axiom (DR-2) that's central to the
framework's substrate-agnosticism claim (DR-3).

I'm reaching out because I'd value a formal reviewer's eye on the
DR-2 compositionality proof before we submit the paper to Nature
Human Behaviour. The proof sketch is in place (closure + budget
additivity + functional composition + associativity), but I'd like
an external pair of eyes on the case analysis for non-commutative
op pairs (in particular `recombine ∘ downscale` vs
`downscale ∘ recombine`).

**Time estimate** : 2-3 hours of your time.

**In return** : courtesy co-authorship on the paper + credit in
CONTRIBUTORS.md for the research program. The paper is scheduled to
be submitted around S20 (June 2026 in our calendar).

**Materials I can share** : draft proof (~3 pages), framework spec
section 6 (axioms DR-0..DR-4), context on the monoid construction.

Would you be available around [DATE — target S6-S8 for circulation
and review] ? Happy to chat via email or a short call beforehand if
that's easier.

Best regards,
Clement Saillant
L'Electron Rare, France
clement@saillant.cc

---

**Footer note** : link to preprint once arXiv'd at S18, link to OSF
pre-registration once locked at S3.
```

## Step S3.3.3 — Commit S3.3

- [ ] Commit :

```bash
git add ops/formal-reviewer-recruitment.md ops/formal-reviewer-email-template.md
git commit -m "docs(tcol): add formal reviewer recruitment"
```

Subject : 45 chars.

Body (2 paragraphs) :

1. Explique la raison d'être : critic finding Q_CR.1 b demande
   reviewer humain externe pour DR-2 proof au-delà du sub-agent
   `critic`. Tracker + email template pour sourcing S3-S5.

2. Explique le fallback : si aucun reviewer confirmé par S6, Pivot
   B partial activé (Paper 1 "formal-leaning", cible PLoS Comp
   Bio). Decision tree documenté, pas improvisée.

---

# Task S3.4 — Retained benchmark 500 items gelé (TDD)

**Goal** : créer un sous-ensemble stratifié de 500 items depuis mega-v2 (498K examples, 25 domaines), gelé par hash SHA-256. Utilisé pour invariant S1 (retained non-regression) et I1 (episodic conservation tests).

**Files:**
- Create : `harness/benchmarks/__init__.py` (empty)
- Create : `harness/benchmarks/retained/__init__.py` (empty)
- Create : `harness/benchmarks/retained/retained.py`
- Create : `harness/benchmarks/retained/items.jsonl` (placeholder 50 items for now)
- Create : `harness/benchmarks/retained/items.jsonl.sha256`
- Create : `tests/unit/test_retained_benchmark.py`

**Note** : nous créons un **placeholder** de 50 items synthétiques pour cycle 1 S3 — la production des 500 items réels depuis mega-v2 requires access to the actual dataset qui est hébergé ailleurs. Le loader + hash checking est la vraie infrastructure ; les 50 items sont remplaçables plus tard sans changer le code.

## Step S3.4.1 — Écrire test failing

- [ ] Créer `tests/unit/test_retained_benchmark.py` avec exactement :

```python
"""Unit tests for retained benchmark loader (TDD, invariants S1+I1)."""
from pathlib import Path

import pytest

from harness.benchmarks.retained.retained import (
    RetainedBenchmark,
    RetainedIntegrityError,
    load_retained,
)


REPO_ROOT = Path(__file__).resolve().parents[2]
BENCH_DIR = REPO_ROOT / "harness" / "benchmarks" / "retained"


@pytest.fixture
def bench() -> RetainedBenchmark:
    return load_retained(BENCH_DIR)


def test_benchmark_loads(bench: RetainedBenchmark) -> None:
    assert len(bench.items) >= 50


def test_benchmark_hash_matches_frozen(bench: RetainedBenchmark) -> None:
    # Loader must verify items.jsonl SHA-256 against items.jsonl.sha256
    assert bench.hash_verified is True


def test_benchmark_items_have_required_fields(
    bench: RetainedBenchmark,
) -> None:
    for item in bench.items:
        assert "id" in item
        assert "context" in item
        assert "expected" in item
        assert "domain" in item


def test_benchmark_items_have_unique_ids(
    bench: RetainedBenchmark,
) -> None:
    ids = [item["id"] for item in bench.items]
    assert len(ids) == len(set(ids))


def test_loader_raises_on_hash_mismatch(tmp_path: Path) -> None:
    # Copy bench files to tmp, corrupt items.jsonl, expect
    # RetainedIntegrityError
    corrupt_items = tmp_path / "items.jsonl"
    corrupt_items.write_text('{"id": "fake", "context": "x", '
                             '"expected": "y", "domain": "z"}\n')
    original_hash = (BENCH_DIR / "items.jsonl.sha256").read_text()
    (tmp_path / "items.jsonl.sha256").write_text(original_hash)
    with pytest.raises(RetainedIntegrityError):
        load_retained(tmp_path)
```

- [ ] Lancer le test, vérifier fail :

```bash
uv run pytest tests/unit/test_retained_benchmark.py -v --no-cov
```

Expected: FAIL avec `ModuleNotFoundError: harness.benchmarks`.

## Step S3.4.2 — Créer items.jsonl placeholder et hash

- [ ] Créer `harness/benchmarks/__init__.py` (vide).
- [ ] Créer `harness/benchmarks/retained/__init__.py` (vide).

- [ ] Créer un script Python helper one-shot pour générer 50 items synthétiques stratifiés sur 5 domaines × 10 items :

```bash
uv run python <<'EOF'
import json
import hashlib
from pathlib import Path

bench_dir = Path("harness/benchmarks/retained")
bench_dir.mkdir(parents=True, exist_ok=True)

domains = ["arithmetic", "syntax", "lexical", "phonology", "semantic"]
items = []
for domain_idx, domain in enumerate(domains):
    for i in range(10):
        idx = domain_idx * 10 + i
        items.append({
            "id": f"retained-{idx:04d}",
            "context": f"Example context {idx} for {domain}",
            "expected": f"Expected output {idx}",
            "domain": domain,
        })

# Write JSONL (one JSON object per line)
items_path = bench_dir / "items.jsonl"
with items_path.open("w", encoding="utf-8") as f:
    for item in items:
        f.write(json.dumps(item, ensure_ascii=False) + "\n")

# Compute and write hash
content = items_path.read_bytes()
digest = hashlib.sha256(content).hexdigest()
(bench_dir / "items.jsonl.sha256").write_text(digest + "\n")

print(f"Wrote {len(items)} items, hash: {digest[:16]}...")
EOF
```

Expected output: `Wrote 50 items, hash: <16hex>...`

## Step S3.4.3 — Implémenter retained.py

- [ ] Créer `harness/benchmarks/retained/retained.py` avec exactement :

```python
"""Retained benchmark loader with SHA-256 integrity check.

Used by invariants S1 (retained non-regression) and I1 (episodic
conservation). The benchmark is frozen : items.jsonl is paired with
items.jsonl.sha256 and loaded via hash-verified read.

Corruption or unauthorized modification of items.jsonl raises
RetainedIntegrityError, blocking the run.
"""
import hashlib
import json
from dataclasses import dataclass, field
from pathlib import Path


class RetainedIntegrityError(Exception):
    """Raised when items.jsonl SHA-256 does not match frozen hash."""


@dataclass(frozen=True)
class RetainedBenchmark:
    """Frozen retained benchmark with integrity check."""

    items: list[dict]
    hash_verified: bool
    source_hash: str


def _compute_sha256(path: Path) -> str:
    return hashlib.sha256(path.read_bytes()).hexdigest()


def load_retained(bench_dir: Path) -> RetainedBenchmark:
    """Load retained benchmark from `bench_dir`.

    Expects:
    - `{bench_dir}/items.jsonl` : one JSON object per line
    - `{bench_dir}/items.jsonl.sha256` : hex digest (first token on
      line)

    Raises RetainedIntegrityError if hash does not match.
    """
    items_path = bench_dir / "items.jsonl"
    hash_path = bench_dir / "items.jsonl.sha256"

    if not items_path.exists():
        raise FileNotFoundError(f"items.jsonl not found at {items_path}")
    if not hash_path.exists():
        raise FileNotFoundError(
            f"items.jsonl.sha256 not found at {hash_path}"
        )

    actual_hash = _compute_sha256(items_path)
    expected_hash = hash_path.read_text().strip().split()[0]

    if actual_hash != expected_hash:
        raise RetainedIntegrityError(
            f"SHA-256 mismatch at {items_path}: "
            f"expected {expected_hash[:16]}..., "
            f"got {actual_hash[:16]}..."
        )

    items = []
    with items_path.open("r", encoding="utf-8") as fh:
        for line in fh:
            line = line.strip()
            if not line:
                continue
            items.append(json.loads(line))

    return RetainedBenchmark(
        items=items,
        hash_verified=True,
        source_hash=actual_hash,
    )
```

- [ ] Lancer les tests, vérifier pass :

```bash
uv run pytest tests/unit/test_retained_benchmark.py -v --no-cov
```

Expected: 5 passed.

- [ ] Lancer full suite pour vérifier coverage :

```bash
uv run pytest
```

Expected : tous tests précédents + 5 nouveaux passent, coverage ≥90%.

## Step S3.4.4 — Commit S3.4

- [ ] Commit :

```bash
git add harness/benchmarks/ tests/unit/test_retained_benchmark.py
git commit -m "feat(bench): add retained benchmark w/ hash check"
```

Subject : 49 chars.

Body (3 paragraphs) :

1. Explique la raison d'être : invariants S1 (retained non-
   regression) et I1 (episodic conservation) requièrent un
   benchmark frozen pour comparaison pre/post-swap stable.

2. Explique le hash-gel SHA-256 : `items.jsonl.sha256` paire avec
   `items.jsonl`, loader vérifie integrity avant consumption,
   raises `RetainedIntegrityError` sur corruption. Supporte le
   contrat de reproducibilité R1.

3. Explique le placeholder : 50 items synthétiques stratifiés sur
   5 domaines × 10 items pour cycle 1 S3. Les 500 items réels de
   mega-v2 seront intégrés en S5+ quand la dataset access est
   prête. Infrastructure loader immuable.

---

# Task S4.1 — fMRI schema lock basé sur Branch A

**Goal** : formaliser le **contrat de données fMRI** pour M2.b, basé sur la décision Branch A (Studyforrest) lockée S2.1. Consommé par la pipeline RSA future (S9-S14).

**Files:**
- Create : `docs/interfaces/fmri-schema.yaml`

## Step S4.1.1 — Créer fmri-schema.yaml

- [ ] Créer `docs/interfaces/fmri-schema.yaml` avec exactement :

```yaml
# fMRI schema for M2.b RSA alignment
# Locked at S4.1 based on G1 Branch A (Studyforrest)
# Reference: docs/feasibility/studyforrest-rsa-note.md

version: "C-v0.3.1+STABLE"
data_source: studyforrest
branch: A  # GO-STUDYFORREST from G1 decision

dataset:
  name: Studyforrest
  version: "2.0.0"  # pin current version at lock time
  license: PDDL
  access: open  # BIDS + DataLad, no auth required
  canonical_url: https://www.studyforrest.org

  format:
    structure: BIDS
    transport: DataLad
    file_format: nifti

parcellations:
  - name: FreeSurfer
    rois_required:
      - STG  # Superior Temporal Gyrus (auditory + lexical)
      - IFG  # Inferior Frontal Gyrus (syntax)
      - AG   # Angular Gyrus (semantic)
  - name: Shen-268
    rois_required:
      - language_network  # Shen-268 parcels overlapping STG/IFG/AG

stimulus_annotations:
  granularity_available:
    - phoneme_level  # 66,611 phonemes timed
    - word_level     # 16,187 words timed
    - sentence_level # 2,528 sentences
    - scene_level    # narrative scene segmentation

  ortho_species_mapping:
    rho_phono: phoneme_level
    rho_lex: word_level
    rho_syntax: sentence_level
    rho_sem: word_level + context_window_300d

  vector_embeddings:
    dimensions: 300
    provenance: Studyforrest STOP annotation
    mappable_to_kiki: true

rdm_computation:
  method: pearson_correlation
  framework: nilearn
  determinism: cpu_nilearn_seeded  # contract R1
  rdm_size_floor: 50  # minimum conditions
  rdm_size_actual: ">= 1000 unique events per ROI (above floor)"

alignment:
  metric: M2.b
  formula: "Pearson(RDM(kiki_activations), RDM(fMRI_ROI))"
  per_profile: true  # H3 monotonic ordering P_min < P_equ < P_max

caveats:
  - Studyforrest rois_required not pre-packaged as language ROIs;
    must extract via FreeSurfer/Shen-268 parcellation post-hoc.
  - Mapping rho_sem -> context window is approximate; must document
    choice in paper 1 methods section.
  - Phoneme-level timing from STOP annotation is aligned to audio
    track, not post-hoc transcription.

future_migrations:
  - If S5-S8 T-Col outreach succeeds with dedicated fMRI lab,
    extend schema to include lab-specific ROIs as additional
    alternative source. Keep Studyforrest as default fallback.
  - If Branch B (PIVOT-SYNTHETIC) is ever activated, add
    synthetic_benchmark section with EEG linguistic corpora
    reference.
```

## Step S4.1.2 — Commit S4.1

- [ ] Commit :

```bash
git add docs/interfaces/fmri-schema.yaml
git commit -m "docs(fmri): lock schema from G1 Branch A"
```

Subject : 42 chars.

Body (2 paragraphs) :

1. Explique le verrouillage : schema fMRI basé sur décision Branch
   A (Studyforrest) prise à S2.1 G1. Cite les 4 checks passés
   (ROIs, stimuli, RDM count, license). Mapping ortho-species
   aligné avec granularités d'annotation Studyforrest (phoneme /
   word / sentence / context-window-300d).

2. Explique les caveats : ROIs extraites post-hoc (FreeSurfer /
   Shen-268), mapping rho_sem approximatif documenté en methods
   section. Future migrations vers lab dédié préservent
   Studyforrest comme fallback permanent.

---

# Task S4.2 — C v0.5 draft tagged (DualVer bump)

**Goal** : bumper DualVer de C-v0.3.1+STABLE → C-v0.5.0+STABLE pour marquer la fin de la phase setup (S1-S4) et l'entrée en phase implémentation (S5+). Introduit changelog + mise à jour des références version.

**Files:**
- Create : `docs/drafts/C-v0.5-changelog.md`
- Modify : `docs/specs/2026-04-17-dreamofkiki-framework-C-design.md` (Version header)
- Modify : `docs/interfaces/primitives.md` (Version header)
- Modify : `docs/interfaces/eval-matrix.yaml` (version field)
- Modify : `docs/interfaces/fmri-schema.yaml` (version field)

## Step S4.2.1 — Créer changelog

- [ ] Créer `docs/drafts/C-v0.5-changelog.md` avec exactement :

```markdown
# C-v0.5.0+STABLE Changelog

**Bump type** : MINOR (FC axis)
**From** : C-v0.3.1+STABLE
**To** : C-v0.5.0+STABLE
**EC status** : STABLE (all property tests pass, no empirical
invalidation)
**Date** : 2026-04-17 (S4.2)

## Changes

### Added (MINOR)

- **Conformance Criterion fully documented** (§6.2) — signature
  typing + axiom tests + BLOCKING invariants enforceable,
  resolving critic finding #3.
- **Typed Protocols exposed** (`kiki_oniric.core.primitives`) —
  8 Protocols satisfying Conformance Criterion condition (1) for
  kiki-oniric substrate.
- **Interface contracts locked** — `docs/interfaces/primitives.md`
  (human-readable) and `docs/interfaces/eval-matrix.yaml` (machine
  readable), tested via `harness.config.eval_matrix.EvalMatrix`.
- **fMRI schema locked** — `docs/interfaces/fmri-schema.yaml`
  pinned to Studyforrest Branch A from G1 decision.
- **Retained benchmark infrastructure** — SHA-256 integrity check
  loader at `harness.benchmarks.retained`, 50-item synthetic
  placeholder, 500-item real set deferred to S5+.
- **OSF pre-registration drafted** — H1-H4 operationalized with
  statistical tests (paired t, TOST, Jonckheere-Terpstra), upload
  checklist separate.
- **T-Col tracking** — fMRI labs + formal reviewer candidates
  registered with weekly status log protocol.
- **G1 gate locked Branch A** — feasibility note with 4 passed
  checks.

### Changed (no FC impact)

- Eval matrix now references `studyforrest` data source for M2.b
  explicitly (was `TBD` before Branch A lock).
- Reviewer fallback protocol documented if Q_CR.1 b human reviewer
  unavailable by S6.

### Not changed

- DR-0..DR-4 axiom statements (unchanged from C-v0.3.1).
- Invariants I/S/K statements (unchanged).
- Swap worktree protocol (unchanged, implementation S7).
- Profile inclusion chain (unchanged).

## Empirical consistency

- No experiments run yet (pre-S5). Contract tests passing :
  - 5 tests in `tests/` (registry 2 + conformance 3)
  - eval-matrix parser 6 tests (S3.1 adds 6 more → 11 total at tag)
  - retained benchmark 5 tests (S3.4 adds 5 more → 16 total at tag)

- EC status : **STABLE** — no invalidation of prior results
  possible since no empirical results exist yet.

## Compat suite results

T-Ops compat suite stratified matrix : N/A at this bump (no MAJOR,
no prior experimental data to re-verify). EC stays STABLE.

## Next bump target

- C-v0.7.0+STABLE at S6 (DR-2 proof draft G3-draft)
- C-v1.0.0+STABLE at S12 (P_equ fonctionnel G4, full stratified
  matrix MAJOR tag)
- C-v1.0.x patches through S18 (PUBLICATION-READY gate G5)
```

## Step S4.2.2 — Modifier version header framework spec

- [ ] Modifier `docs/specs/2026-04-17-dreamofkiki-framework-C-design.md` ligne 3 :

  Remplacer :
  ```
  **Version** : v0.1.0-draft
  ```

  Par :
  ```
  **Version** : C-v0.5.0+STABLE
  ```

## Step S4.2.3 — Modifier version header primitives.md

- [ ] Modifier `docs/interfaces/primitives.md` :

  Remplacer :
  ```
  **Version** : C-v0.3.1+STABLE (draft, bump to C-v0.5.0 at S4.2)
  ```

  Par :
  ```
  **Version** : C-v0.5.0+STABLE
  ```

## Step S4.2.4 — Modifier version dans YAML interfaces

- [ ] Dans `docs/interfaces/eval-matrix.yaml`, remplacer :
  ```
  version: "C-v0.3.1+STABLE"
  ```
  Par :
  ```
  version: "C-v0.5.0+STABLE"
  ```

- [ ] Dans `docs/interfaces/fmri-schema.yaml`, remplacer :
  ```
  version: "C-v0.3.1+STABLE"
  ```
  Par :
  ```
  version: "C-v0.5.0+STABLE"
  ```

## Step S4.2.5 — Run full suite pour vérifier compat

- [ ] Lancer tous les tests :

```bash
uv run pytest
```

Expected : tous tests passent (registry 2 + conformance 3 + eval_matrix 6 + retained 5 = 16 tests). Le test `test_matrix_version_starts_with_C_prefix` passe naturellement puisque `C-v0.5.0+STABLE` matche le pattern.

## Step S4.2.6 — Commit S4.2

- [ ] Commit :

```bash
git add docs/drafts/C-v0.5-changelog.md \
        docs/specs/2026-04-17-dreamofkiki-framework-C-design.md \
        docs/interfaces/primitives.md \
        docs/interfaces/eval-matrix.yaml \
        docs/interfaces/fmri-schema.yaml
git commit -m "refactor(ver): bump framework to C-v0.5.0 STABLE"
```

Subject : 48 chars.

Body (3 paragraphs) :

1. Explique le bump MINOR (FC axis) : ajouts Conformance Criterion
   (§6.2), typed Protocols, interface contracts locked,
   infrastructure benchmark + OSF + T-Col. EC reste STABLE
   (aucune expérience à invalider).

2. Explique les 5 fichiers touchés : changelog nouveau, 4
   versioning headers updated (framework spec, primitives.md,
   eval-matrix.yaml, fmri-schema.yaml). Cohérence DualVer
   maintenue cross-document.

3. Explique les prochains bumps ciblés : C-v0.7.0 à S6 (DR-2
   draft), C-v1.0.0 à S12 (P_equ G4 MAJOR tag). Rythme de bump
   aligné avec gates G2-G5.

---

# Self-review

**1. Spec coverage** — chaque item du plan original Phase 2 S3-S4
est couvert :
- S3.1 (interfaces formels) → Task S3.1 ✅
- S3.2 (OSF pre-registration) → Task S3.2 ✅
- S3.3 (recruter relecteur) → Task S3.3 ✅
- S3.4 (retained benchmark TDD) → Task S3.4 ✅
- S4.1 (fMRI schema) → Task S4.1 ✅
- S4.2 (C v0.5 tag) → Task S4.2 ✅

**2. Placeholder scan** — aucun TBD / TODO / "fill in later"
non-intentionnel. Les `[TBD1]`, `[TBD2]`, `[TBD3]` dans S3.3 sont
**délibérés** — remplis par l'utilisateur avec ses contacts
réels, pas par un subagent.

**3. Type consistency** — vérifié :
- `EvalMatrix` dataclass (S3.1) référencé dans test_eval_matrix.py (S3.1)
- `RetainedBenchmark` + `RetainedIntegrityError` (S3.4) référencés dans test_retained_benchmark.py (S3.4)
- Version strings cohérentes `C-v0.5.0+STABLE` dans les 5 fichiers modifiés (S4.2)
- Paths `docs/interfaces/` consistent avec framework spec §11 cross-track interfaces table

**4. Commit count attendu** : 6 commits (1 par task).

**5. Validator risks** :
- S3.1 subject `feat(config): lock interfaces primitives + eval matrix` = 49 chars ≤50 ✅
- S3.2 subject `docs(osf): draft H1-H4 pre-registration` = 41 chars ≤50 ✅
- S3.3 subject `docs(tcol): add formal reviewer recruitment` = 45 chars ≤50 ✅
- S3.4 subject `feat(bench): add retained benchmark w/ hash check` = 49 chars ≤50 ✅
- S4.1 subject `docs(fmri): lock schema from G1 Branch A` = 42 chars ≤50 ✅
- S4.2 subject `refactor(ver): bump framework to C-v0.5.0 STABLE` = 48 chars ≤50 ✅

Toutes subject lines conformes.

---

**End of S3-S4 atomic plan.**

**Version** : v0.1.0
**Generated** : 2026-04-17 via superpowers:writing-plans skill
**Source** : refinement of Phase 2 S3-S4 from
`docs/superpowers/plans/2026-04-17-dreamofkiki-implementation.md`
